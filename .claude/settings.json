{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "if echo $CLAUDE_TOOL_INPUT | grep -q 'git commit'; then cargo fmt --check && cargo clippy --workspace -- -D warnings && cargo test --workspace; fi"
          }
        ]
      }
    ]
  }
}
